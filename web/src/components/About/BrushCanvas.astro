    <div class="content" id="sidebar">

      <div class="content__aside">
        <div class="slider">
          <label for="lazy">Brush size</label>
          <input class="range" type="range" id="slider_brush" name="radius" min="2" max="50" />
        </div>

        <div class="slider">
          <label for="lazy">Smoothing</label>
          <input class="range" type="range" id="slider_lazy" name="lazy" min="2" max="100" />
        </div>
      </div>


      <div class="flex content__bottom">
        <button class="button-disable" id="button_lazy">Smoothing on</button>
        <button class="button-clear" id="button_clear">Clear doodle</button>
      </div>
      
    </div>
    

  <div class="canvas-container" id="canvas_container">
    <canvas class="lazy-canvas" id="canvas_interface"></canvas>
    <canvas class="lazy-canvas" id="canvas_drawing"></canvas>
    <canvas class="lazy-canvas" id="canvas_temp"></canvas>
    <canvas class="lazy-canvas" id="canvas_grid"></canvas>
  </div>

<style is:global>
  #mobileConsole {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 150px;
  background: rgba(255, 255, 255, 0.8);
  overflow: auto;
  z-index: 9999;
  font-family: monospace;
  font-size: 12px;
  padding: 10px;
  border-top: 1px solid #ccc;
  color: black;
}
  #sidebar {
    position: absolute;
    z-index: 20;
  }

  #canvas_interface {
  z-index: 15;
}

  #canvas_grid {
    z-index: 14;
  }

  #canvas_temp {
    z-index: 12;
  }

  #canvas_drawing {
    z-index: 10;
  }

  #canvas_container {
      position: absolute;
      top: 0;
      left: 0;
      bottom: 0;
      right: 0;
      width: 100%;
      height: 100%;
  }
  #slider_lazy, #slider_brush {
    width: 100px;
  }

  .lazy-canvas {
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
  }
  .lazy-canvas {
  touch-action: none;
  -webkit-touch-callout: none;
  -webkit-user-select: none;
  -khtml-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  user-select: none;
}

</style>

<script>
  import Scene from "./classes/Scene";

  const scene = new Scene({
    canvasContainer: "canvas_container",
    sidebar: "sidebar",
    canvas: {
      interface: "canvas_interface",
      drawing: "canvas_drawing",
      temp: "canvas_temp",
      grid: "canvas_grid",
    },
    slider: {
      brush: "slider_brush",
      lazy: "slider_lazy",
    },
    button: {
      lazy: "button_lazy",
      clear: "button_clear",
    },
  });
  scene.init();
  
</script>
