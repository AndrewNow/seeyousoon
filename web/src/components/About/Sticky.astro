---

---

<div class="wrapper" id="sticky-area">
  <div class="content content--sticky content--grid item1">
    <div class="el-inner">
      <h1>
        An expression of farewell, with the intention of meeting once again.
      </h1>
    </div>
  </div>
  <div class="content content--sticky content--grid item2">
    <div class="el-inner">
      <h1>test content 2</h1>
    </div>
  </div>
  <div class="content content--sticky content--grid item3">
    <div class="el-inner">
      <p>
        Welcome to the culinary adventures of our trio of globetrotting chefs!
        With a passion for exploration and a love for diverse flavours, we
        travel the world to bring you the most exciting culinary experiences
        through our residency format.
      </p>
    </div>
  </div>
  <div class="content content--sticky content--grid item4">
    <div class="el-inner">
      <p>test content 4</p>
    </div>
  </div>
</div>

<style lang="scss">
  .wrapper {
    overflow: hidden;
    width: 98%;
    margin: 0 auto;
  }

  .item1 {
    background: var(--beige-300);
  }
  .item2 {
    background: var(--beige-300);
  }
  .item3 {
    background: var(--beige-300);
  }
  .item4 {
    background: var(--beige-300);
  }
  .content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .content--sticky {
    width: 80%;
    margin: 0 auto;
    // position: sticky;
    border: 1px solid var(--grey-100);
    top: 0;
    --offset: 0px;
    top: var(--offset);
    height: calc(100vh - var(--offset));
    border-radius: 20px;
    // transform-origin: 50% 0% 0px;
    transform-origin: 100% 0% 0px;
    /* opacity: 1; */
  }
  .el-inner {
    max-width: 65%;
    margin: 0 auto;
    text-align: center;
    color: var(--brown-300);
    p {
      max-width: 70%;
      margin: 0 auto;
    }
    // opacity: 0;
  }
</style>

<script>
  import { animate, scroll, ScrollOffset, timeline } from "motion";

  const contentElements = [...document.querySelectorAll(".content--sticky")];
  const totalContentElements = contentElements.length;

  contentElements.forEach((el, position) => {
    // const inner = el.querySelector(".el-inner");

    const isLast = position === totalContentElements - 1;
    const offsetVal = isLast
      ? ["start start", "end start"]
      : ["center -100px", "center end"];

    // const scaleVal = isLast ? 1 : [0.5, 1];

    const sequence = [
      [
        el,
        {
          // scale: [1, 0.5],
          // opacity: [null, 0.99, 0.99, 0],
          // y: "50%",
          opacity: 0,
          y: "60%",
          rotate: -20,
          borderRadius: "20px",
          scale: 0.75,
          // filter: "brightness(60%) contrast(135%)",
        },
      ],
    ];

    scroll(timeline(sequence), {
      target: el,
      offset: ["1 1", "1 0"],
    });
  });
</script>
